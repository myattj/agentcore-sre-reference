# Secrets (local environment files are never committed)
.env.local

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/

# CLI Internals
.cli/*

# Per-developer deployment target state
aws-targets.json

# Ephemeral Staging
.cache/*
