# Phase B sandbox runtime deps.
#
# Deliberately minimal — first slice is a dummy "add line to README"
# script. v2 will add `anthropic` (Claude Agent SDK) and possibly
# `uv` for venv-management inside repo clones. Keep this list small
# until the inner Claude loop actually needs more.
#
# pyjwt[crypto] mirrors the agent's coreAgent/pyproject.toml pin.
# Required for RS256 JWT signing in scm_github.py — the [crypto]
# extra pulls in cryptography for the RS256 backend.

boto3>=1.34
pyjwt[crypto]>=2.8
